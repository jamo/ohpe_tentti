<%= form_for(@questions) do |f| %>
<% if @questions.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questions.errors.count, "error") %> prohibited this paste from being saved:</h2>

      <ul>
        <% @questions.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field" >
    <%= f.label :nick %>:
    <%= f.text_field :nick, :size => 30 %><br>

    <%= f.label :email %>:
    <%= f.text_field :email, :size => 30 %><br>
	  Muuta?
    <%= f.text_area :other, :size => 85 %><br>
	</div>
  <div class="actions center">
    <%= f.submit 'Save' %>
  </div>
<% end %>
